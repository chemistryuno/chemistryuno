{
  "name": "chemistryuno-mendeleef",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "node init.js",
    "start": "node start.js",
    "dev": "pnpm start",
    "build": "node build.js",
    "test": "node test.js",
    "test:build": "node test.js --build",
    "build:backend": "node -e \"const {execSync} = require('child_process'); process.env.CGO_ENABLED='0'; execSync('go build -o chemistryuno' + (process.platform==='win32'?'.exe':'') + ' main.go', {stdio:'inherit'})\"",
    "build:frontend": "pnpm -C frontend build",
    "install:frontend": "pnpm -C frontend install",
    "frontend:dev": "pnpm -C frontend dev",
    "frontend": "pnpm -C frontend install --prefer-offline --silent && pnpm build:frontend && pnpm -C frontend copy-dist && echo \"✅ 前端打包部署完成！\"",
    "backend": "node -e \"const {execSync} = require('child_process'); process.env.CGO_ENABLED='0'; execSync('go run main.go', {stdio:'inherit'})\"",
    "db:init": "node -e \"const {execSync} = require('child_process'); execSync('go run scripts/init_db.go', {cwd:'backend', stdio:'inherit'})\"",
    "db:init-test": "node -e \"const {execSync} = require('child_process'); execSync('go run scripts/setup_test_env.go', {cwd:'backend', stdio:'inherit'})\"",
    "go:test": "node -e \"const {execSync} = require('child_process'); execSync('go test ./...', {stdio:'inherit'})\"",
    "tools:init-db": "node -e \"const {execSync} = require('child_process'); execSync('go run init_db.go', {cwd:'tools', stdio:'inherit'})\"",
    "tools:migrate-creds": "node -e \"const {execSync} = require('child_process'); execSync('go run migrate_credential_ids.go', {cwd:'tools', stdio:'inherit'})\"",
    "clean": "node -e \"const fs = require('fs'); ['chemistryuno.db', 'chemistryuno.exe', 'chemistryuno', 'dist'].forEach(f => { try { if (fs.existsSync(f)) { if (fs.statSync(f).isDirectory()) { fs.rmSync(f, {recursive: true}); } else { fs.unlinkSync(f); } } } catch(e) {} })\"",
    "clean:all": "pnpm clean && node -e \"const fs = require('fs'); ['frontend/node_modules', 'frontend/dist', 'frontend/.vite'].forEach(f => { try { if (fs.existsSync(f)) { fs.rmSync(f, {recursive: true}); } } catch(e) {} })\""
  },
  "description": "Chemistry UNO V1.0.0 Mendeleef - 化学元素版UNO游戏 (正式版)",
  "keywords": ["game", "uno", "chemistry", "vue", "go"],
  "author": "TomKe & Copilot",
  "license": "MIT"
}
